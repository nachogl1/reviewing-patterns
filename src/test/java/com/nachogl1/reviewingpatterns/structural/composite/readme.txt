Create a program that represents a file system with directories and files, where both directories and files can have a
size and a name. Implement the Composite pattern to allow for treating directories and files in the same way, such as
calculating the total size of a directory and its subdirectories, and listing all the files and directories within a directory.